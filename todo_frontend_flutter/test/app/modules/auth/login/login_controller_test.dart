import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:todo_frontend_flutter/app/modules/auth/login/login_controller.dart';
import 'package:todo_frontend_flutter/app/modules/auth/auth_module.dart';

void main() {
  initModule(AuthModule());
  // LoginController login;
  //
  setUp(() {
    //     login = AuthModule.to.get<LoginController>();
  });

  group('LoginController Test', () {
    //   test("First Test", () {
    //     expect(login, isInstanceOf<LoginController>());
    //   });

    //   test("Set Value", () {
    //     expect(login.value, equals(0));
    //     login.increment();
    //     expect(login.value, equals(1));
    //   });
  });
}
